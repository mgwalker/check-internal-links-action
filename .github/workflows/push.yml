name: hi

on: [push]

jobs:
  bob:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: mgwalker/check-internal-links-action@f097bfd1a35c2504f756616a475f597c571a2b89
        with:
          path: pages
          # baseUrl: https://testing.com
          # ignore:
          #   - /admin/**/*
