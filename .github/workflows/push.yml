name: hi

on: [push]

jobs:
  bob:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: mgwalker/check-internal-links-action@e12dc33
        with:
          - path: pages
