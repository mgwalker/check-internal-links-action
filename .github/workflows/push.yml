name: hi

on: [push]

jobs:
  bob:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: mgwalker/check-internal-links-action@e8dff6591f0d5084316fa218ba71340022da09d0
        with:
          path: pages
          # baseUrl: https://testing.com
          # ignore:
          #   - /admin/**/*
